apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "expensestracker.fullname" . }}-test-connection"
  labels:
    {{- include "expensestracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "expensestracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
